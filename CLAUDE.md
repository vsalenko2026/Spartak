# Финмодель УК «Спартак» — правила работы

Финансовая модель управляющей компании сети футбольных академий:
прогноз сети, EBITDA, денежный поток, экономика франшизы, мерч, лагеря,
выплаты ФК, сценарии и чувствительность.

## Жёсткие правила

1. **Партнёры и школы считаются отдельно.** Один партнёр держит несколько
   школ. Правило «1 партнёр = 1 школа» запрещено.
2. **Постепенный набор детей.** Новая школа выходит на мощность по этапам
   1–12 мес. / 13–24 мес. / зрелая. Заменять набор полной загрузкой нельзя.
3. **БДР и БДДС не смешиваются.** Начисление и деньги живут в разных
   блоках `02_Модель`; расхождение между ними объясняется складом, займом
   и авансами, а не «подгоняется».
4. **Все ручные вводные — на листе `01_Вводные`.** Новых вводных на других
   листах не заводить.
5. **Экономика не меняется без подтверждения.** Представление, навигация,
   форматы, подсказки — можно. Формулы, ставки, коэффициенты — только с
   явного согласия владельца модели.
6. **Модель правится скриптом, не руками.** Любая правка — код в `tools/`,
   который детерминированно превращает исходник в результат. Тогда правку
   видно в диффе, можно откатить и пересобрать.

## Контракт правки

Каждое изменение модели описывается пятью пунктами — в коммите и в
`docs/Журнал_правок.md`:

```
Проблема       что не так и где (лист!ячейка)
Причина        почему так получилось
Решение        что именно меняется
Финансовый эффект   дельта EBITDA / cash / детей / школ (числом)
Проверки       какие тесты это ловят
```

Правка без замера финансового эффекта не принимается: любое движение
формулы проверяется прогоном до и после.

## Инструменты

```bash
python3 tools/extract_model.py                    # карта модели -> build/
python3 tools/extract_model.py --cell 02_Модель!B44   # формула + связи ячейки
python3 tools/extract_model.py --sheet 07_Мерч        # карта одного листа
python3 tools/extract_model.py --grep "роялти"        # поиск по формулам и текстам

python3 tools/run_model.py --tag база             # прогон, срез в build/runs/
python3 tools/run_model.py --scenario негативный  # прогон сценария
python3 tools/run_model.py --set '01_Вводные!B58=0.07' --tag ндс7
python3 tools/run_model.py --compare база негатив # построчная дельта

python3 tools/build_user_friendly.py              # V8.8 -> V8.9
python3 tools/build_v9.py                         # V8.9 -> V9.0 (новая структура)
python3 tools/compare_v8_v9.py                    # сверка V9.0 со старой моделью
python3 tools/check_tz.py                         # сверка модели с ТЗ v6.011
```

## Две модели в репозитории

- `model/Spartak_V8_9_понятная.xlsx` — рабочая версия на исходной структуре
  (24 листа, 111 410 формул). Из неё считаются эталон и тесты.
- `model/Spartak_V9_0.xlsx` — та же экономика в новой структуре: 10 листов,
  22 510 формул, пересчёт 4 секунды. Годовые итоги слева, месяцы справа,
  формулы через имена вводных, никаких цепочек «витрина → движок».
  Собирается `tools/build_v9.py`, сверяется `tools/compare_v8_v9.py`
  (59 показателей до копейки).

`build/` не версионируется — пересобирается за полминуты.

## Как устроена модель (то, что уже выяснено)

- 24 листа, ~115 800 заполненных ячеек, ~111 400 формул, 203 имени.
- Горизонт — 60 месяцев, столбцы `B:BI`. Даты строит `02_Модель!B3`
  от старта `01_Вводные!B7`.
- `02_Модель` — витрина: БДР (строки 17–47), БДДС (50–72), контроль
  оборотного капитала (74–78). Своих расчётов почти не содержит, тянет
  из `04`–`10`.
- `19_Расчет_аналитики` (4857 строк) — расчётный движок и 76 % всех формул
  книги: **пять полных копий модели** с шагом 1012 строк. Три сценарные
  (негативный 338–1349, базовый 1350–2361, позитивный 2362–3373) питают
  `03_Сценарии`, две дополнительные (3374–4385, 4386–4857) — лист
  `12_Чувствительность`, и считаются только при `B234` = «Да».
  Каждая копия — около 16 800 формул, а наружу из неё смотрят 22 строки
  (у копий чувствительности — 9). Разбор, что с этим делать, —
  в `docs/Где_облегчить_структуру.md`. 11.09.2026 из каждой копии убраны
  десять мёртвых строк, а два контроля, считавшихся в стол, выведены на
  `13_Проверки` (строки 77–80).
- **Вводные доезжают не всюду.** Витрина и движок считают одно и то же
  разными формулами, поэтому новая вводная может быть подключена только к
  одной из них. `B22` (школ на партнёра) был подключён только к витрине, и
  при значении 1 это не проявлялось. Меняя любой коэффициент, сверяйтесь с
  `13_Проверки` строки 34, 35, 48 — они сравнивают витрину со сценарием.
- **`01_Вводные!B192` хранит текст** — «Негативный» / «Базовый» /
  «Позитивный», а `B194` переводит его в номер:
  `=IF(B192=C197,1,IF(B192=D197,2,3))`. Подстановка числа не ломается
  явно — она молча проваливается в последнюю ветку и даёт позитивный
  сценарий. `run_model.py` поэтому сверяет посчитанный номер с
  запрошенным и валит прогон при расхождении.
- Пересчёт делает LibreOffice Calc (openpyxl формулы не считает).
  Полная книга пересчитывается ~12 секунд.

## Базовые цифры (сценарий «Базовый», 60 мес.)

| Показатель | Значение |
|---|---|
| Выручка без НДС, сумма | 2 241 351 135 |
| EBITDA, сумма | 763 929 136 |
| Деньги на конец | 476 204 153 |
| Минимальный остаток за период | −3 379 030 |
| Активных детей на конец | 3 835 |
| Новых школ на конец | 429 |

Числа обновляются только вместе с объяснением, что именно их сдвинуло.

## Открытые вопросы (решает владелец модели, не агент)

Закрыты: закрытие школ — 3 %/год (`B13`, 11.09.2026); каникулы по роялти —
штатный параметр `B297`, по умолчанию 0; страховые взносы — 30 %, льгот нет
(`B131`, 11.09.2026); целевой запас формы — 4 мес. (`B80`, 10.09.2026);
школ на нового партнёра — 1,5 (`B22`, 11.09.2026); начало регулярного
пополнения склада — 01.11.2027 (`B81`, 11.09.2026).

Остаются:

1. **Конверсия продажи в открытие** — `B298` = 100 %, в v4 было 95 %
   (цена вопроса −32,4 млн EBITDA).
2. **Премия Евгения** — gross или полный бюджет (`B132` = 0,87, «Рабочее»).
3. **Пошкольный расчёт действующей сети** — считается котлом.
4. **Месяцы закупок склада** — `B82:B85` = 2/5/8/11, статус «Рабочее».
5. **Чем финансируется кассовый разрыв** — строки привлечения денег в БДДС
   нет, проценты не начисляются. Сейчас восемь месяцев с отрицательной
   кассой, глубина до −3,4 млн.
6. **Когда включать лагеря** — `B241` = «Нет» с 09.09.2026.
7. **Стартовые юрлица** — ТЗ v6.011 говорит 49, реестр сети от 21.08.2026 —
   51. Модель считает по реестру.
8. **Порог возврата займа** привязан к целевому запасу (`B80` = 4 мес.),
   а ТЗ требует шести. Развязывается отдельной вводной.

Агент может подготовить по каждому вариант с расчётом последствий, но не
выбирает сам.
