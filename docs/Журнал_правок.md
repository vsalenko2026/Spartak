# Журнал правок финмодели

Каждая правка описывается пятью пунктами. Правка без замера финансового
эффекта в журнал не попадает — и, значит, не считается сделанной.

Формат записи:

```
## <дата> — <короткое название>

Проблема            что не так и где (лист!ячейка)
Причина             почему так получилось
Решение             что именно изменилось
Финансовый эффект   дельта EBITDA / деньги / дети / школы, числом
Проверки            какие тесты это ловят
```

---

## 10.09.2026 — заведён контур проверки

Проблема
: модель нельзя было проверить иначе, как открыв её в Excel и посмотрев
глазами. Ошибку в одной из 114 118 формул так не находят.

Причина
: не было ни машинного чтения книги, ни способа её пересчитать вне Excel,
ни тестов на экономические тождества.

Решение
: добавлены карта модели (`tools/extract_model.py`), прогон через
LibreOffice (`tools/run_model.py`), статический аудит
(`tools/audit_model.py`), 38 тестов (`tests/`), эталон ключевых цифр
(`tools/baseline.py`) и самопроверка контура (`tools/selfcheck.py`).

Финансовый эффект
: нулевой. Ни одна формула и ни одно значение модели не изменены; V8.9
осталась ровно такой, какой была.

Проверки
: `python3 -m pytest tests -q` — 38 тестов; `python3 tools/selfcheck.py` —
шесть подсадных дефектов, все ловятся.

---

## 10.09.2026 — целевой запас формы 6 месяцев → 4

Проблема
: `01_Вводные!B80` = 6 месяцев. Закупка при этом идёт четыре раза в год
(окна `B82:B85` = февраль, май, август, ноябрь), то есть раз в три месяца.
Запас на шесть месяцев вперёд при интервале в три — двойной буфер: в
среднем 45 млн ₽ лежат на складе без движения.

Тяжелее всего майское окно: шестимесячный горизонт от мая захватывает
весь старт сезона (август-октябрь дают 85 % объёма). В деньгах это 71 млн
в 2029-м, 106 млн в 2030-м, 141 млн в 2031-м. Оплата при кредиторке в
15 дней разбивается пополам между маем и июнем, а поступления в эти
месяцы — 21 и 16 млн. Касса уходила в минус с мая по август 2029 года,
дно — 39 969 365 ₽ в июне. Закрыть его в модели нечем: кредитной линии
нет, займ ФК гасится по своему графику.

Причина
: горизонт запаса не был связан с частотой закупок. Блок и не проходил
финального согласования — у месяцев закупок стоял статус «Рабочее» и
комментарий «уточнить точные месяцы закупок» (открытый вопрос №7).

Решение
: `B80` = 4 = три месяца до следующей закупки плюс месяц страхового
запаса; срок поставки формы это позволяет (подтверждено 10.09.2026).
Майское окно теперь закрывает потребность до сентября, а октябрь и
ноябрь уезжают в августовское окно — туда, где есть выручка (58 млн
поступлений в августе против 24 в мае).

Заодно комментарий `E80` зашивал «6 месяцев» текстом — сделан следящим за
`B80`, чтобы при следующей смене параметра подпись не соврала. Статус
`D80` дополнен датой уточнения.

Месяцы закупок не менялись. Проверены сдвиги окон на 3/6/9/12 и 1/4/7/10:
разрыв не уменьшается, а сеть остаётся без формы на 8,7 и 27,7 млн —
майская закупка идёт как раз под старт сезона.

Финансовый эффект
: из четырнадцати ключевых цифр сдвинулись две.

| Показатель | Было | Стало | Дельта |
|---|---:|---:|---:|
| Пиковый кассовый дефицит | −39 969 365 | −6 764 541 | +33 204 824 |
| Деньги на конец горизонта | 260 643 259 | 294 528 672 | +33 885 412 |
| EBITDA | 508 278 045 | 508 278 045 | 0 |
| Выручка без НДС | 1 795 953 466 | 1 795 953 466 | 0 |
| Активных детей на конец | 2 956 | 2 956 | 0 |
| Новых школ на конец | 308 | 308 | 0 |
| Выплаты ФК | 192 501 338 | 192 501 338 | 0 |

EBITDA не двинулась ни на рубль — склад это оборотный капитал, а не
расход. Месяцев с отрицательной кассой стало 11 вместо 14.

Проверки
: `pytest tests` — 38 тестов, зелёные. `tools/audit_model.py` — важных
замечаний ноль. Необеспеченный спрос (`07_Мерч!27`) равен нулю во всех
шестидесяти месяцах: сети хватает формы. Сборщик подтвердил, что кроме
семи намеренных правок в книге не изменилось ничего. Эталон принят
`tools/baseline.py --update`.

---

## 11.09.2026 — закрытие школ 3 % в год; каникулы по роялти как штатный параметр

Проблема
: закрытие школ числилось открытым вопросом №1, хотя механика в модели уже
была: `01_Вводные!B13` — «Закрытие школ / юрлиц, %/год» со статусом
«Не задано» и значением 0, а `B14 = (1−B13)^(1/12)` — месячный коэффициент
сохранения. Он применяется и к действующей сети (`06_Сеть_и_дети!11`), и к
юрлицам (строка 10), и ко всем когортам новых школ (строки 54–77). При нуле
всё это работало вхолостую: сеть в модели не теряла ни одной школы за пять
лет.

То же с каникулами по роялти: `B297` подключён к расчёту (на него ссылаются
19 740 ячеек), но стоял со статусом «Уточнить», и по подписи нельзя было
понять, параметр это или незакрытый вопрос.

Причина
: параметры завели заранее, под будущее решение, но статусы остались
черновыми — и оба вопроса продолжали числиться нерешёнными, хотя
требовалось только значение.

Решение
: `B13` = 3 % в год (согласовано 11.09.2026). Подпись `E13` сделана следящей
за `B13`: при нуле она говорит «механика работает, пока 0 %», при любом
другом значении называет ставку. Статус `D13` — «Согласовано 11.09.2026».

`B297` оставлен со значением 0 по умолчанию, статус сменён на «Параметр; по
умолчанию 0», подпись сделана следящей: при нуле объясняет, как включить,
при ненулевом — сколько месяцев роялти не начисляется.

Заодно снят статус «Временно» со страховых взносов `B131`: ставка остаётся
30 %, льготных режимов у компании нет (подтверждено 11.09.2026).

Финансовый эффект
: закрытие 3 % в год — правка экономическая, и она ожидаемо двигает почти
всё. Каникулы и статус взносов на цифры не влияют: значения не менялись.

| Показатель | Было | Стало | Дельта |
|---|---:|---:|---:|
| Выручка без НДС | 1 795 953 466 | 1 705 433 983 | −90 519 484 |
| EBITDA | 508 278 045 | 458 984 065 | −49 293 980 |
| Прибыль после УСН | 425 205 031 | 383 051 957 | −42 153 074 |
| Деньги на конец | 294 528 672 | 263 731 413 | −30 797 258 |
| Минимальный остаток | −6 764 541 | −7 576 687 | −812 146 |
| Активных детей на конец | 2 956 | 2 685 | −271 |
| Новых школ на конец | 308 | 286 | −22 |
| Активных юрлиц на конец | 371 | 340 | −31 |
| Паушальные | 332 186 997 | 332 186 997 | 0 |

Паушальные не изменились: продажи франшиз те же, закрытие касается уже
открытых школ.

Проверки
: 38 тестов зелёные. Тест «школы не исчезают скачком» заменён на
`test_закрытие_школ_применяется_как_задано`: он читает ставку из среза
прогона и требует, чтобы действующая сеть убывала ровно коэффициентом
`(1−B13)^(1/12)` — ни быстрее, ни медленнее. Это ловит и потерю закрытия, и
его двойное применение. Проверено подсадным дефектом «коэффициент
сохранения оторван от ставки» (`tools/selfcheck.py --only закрытие`).

`run_model.py` теперь кладёт в срез ключевые вводные, поэтому тесты видят,
в каком режиме считалась модель, а не только её результат.

Аудит без важных замечаний; сборщик подтвердил 14 намеренных правок и ноль
незапланированных. Эталон принят.

---

## 11.09.2026 — школ на нового партнёра 1 → 1,5 (и починка связи)

Проблема
: `01_Вводные!B22` = 1 со статусом «Временно»: каждая продажа франшизы
открывала ровно одну школу. Это противоречило правилу модели «партнёр ≠
школа» — действующая сеть живёт с коэффициентом 2,04 (104 школы на 51
партнёра).

При попытке поставить другое значение вскрылся дефект: **`B22` был
подключён только к витрине.** В `06_Сеть_и_дети!8` он используется 60 раз,
в сценарном движке `19_Расчет_аналитики` — ни разу, хотя движок считает
открытия школ своей формулой. Пока значение было равно единице, разрыва не
существовало: умножение на 1 ничего не меняет. Как только коэффициент стал
1,5, витрина и сценарии разъехались, и лист `13_Проверки` показал это
сразу — строки 34, 35 и 48 («сценарий совпадает с EBITDA / деньгами /
CAPEX») стали красными.

Второй разрыв нашёлся там же: в самой витрине хвост `BJ8:BO8` — шесть
месяцев за горизонтом, по которым планируется целевой запас склада, — тоже
считался без `B22`. Склад закупался под заниженное число школ.

Причина
: параметр завели позже, чем построили движок и хвост, и не проверили, что
он доехал до всех мест. Единица маскировала разрыв.

Решение
: `B22` = 1,5 (согласовано 11.09.2026). `B22` проброшен в открытия школ во
всех пяти копиях движка (строки 363, 1375, 2387, 3399, 4411 — 330 формул) и
в хвост витрины `06_Сеть_и_дети!BJ8:BO8` (6 формул).

Безопасность проброса доказана отдельным прогоном: сначала связь была
восстановлена при `B22` = 1, и сравнение с прежним состоянием дало **пустую
таблицу дельт** — ни одна строка не изменилась. Только после этого значение
стало 1,5.

Финансовый эффект
:

| Показатель | Было | Стало | Дельта |
|---|---:|---:|---:|
| Выручка без НДС | 1 705 433 983 | 2 241 351 135 | +535 917 153 |
| EBITDA | 458 984 065 | 763 929 136 | +304 945 071 |
| Прибыль после УСН | 383 051 957 | 644 563 701 | +261 511 744 |
| Деньги на конец | 263 731 413 | 476 204 153 | +212 472 740 |
| Минимальный остаток | −7 576 687 | −3 379 030 | +4 197 657 |
| Активных детей на конец | 2 685 | 3 835 | +1 150 |
| Новых школ на конец | 286 | 429 | +143 |
| Активных юрлиц на конец | 340 | 340 | 0 |
| Паушальные | 332 186 997 | 332 186 997 | 0 |

Юрлица и паушальные не изменились — продаж франшиз столько же, меняется
только число школ на партнёра. Это сходится с тем, что `B22` по смыслу
влияет на школы, а не на продажи.

Проверки
: 37 тестов зелёные, четыре контроля согласованности витрины и движка
(`13_Проверки` строки 9, 34, 35, 48) вернулись на ноль. Сценарии снова
упорядочены: EBITDA негативный 447,3 < базовый 763,9 < позитивный 1 149,8
млн. Аудит без важных замечаний. Эталон принят.

**Красным остаётся `test_склад_не_отрицательный`** — см. следующий раздел.

---

## 11.09.2026 — уборка мёртвых строк, контроли выведены на лист проверок

Проблема
: в каждой из пяти копий движка `19_Расчет_аналитики` было 60 строк,
которые пересчитывались всегда, но их результат не читала ни одна ячейка
книги. Среди них — три вещи, которые выглядели как контроли, но ими не были:

- строка `+1` от начала копии задумывалась как сверка продаж движка с
  листом `04_Продажи`, но ссылки в ней разъехались при протяжке. В первом
  столбце сравнивались разные периоды, дальше формула сравнивала движок сам
  с собой, а к концу горизонта уезжала и левая граница окна — с `$B` на
  `$AX`. Показывала она не расхождение, а продажи текущего месяца: 7, 13, 16
  по сценариям;
- строка `+224` сравнивала ячейку с её же формулой — тавтология, всегда 0;
- строка `+223` была настоящим и работающим контролем (сумма школ по 25
  месяцам жизни против суммы трёх стадий), но её результат тоже никуда не
  выводился.

Остальное — дубли (второй расчёт новых юрлиц, копия строки школ) и пустые
заготовки, возвращавшие пустоту.

Причина
: следы прошлых версий. Копии движка размножались целиком, вместе с
отладочными строками и черновиками, и каждая ошибка тиражировалась пятикратно.

Решение
: очищены десять строк в каждой из пяти копий — 2 720 формул. Строки
оставлены на месте пустыми: удалять их нельзя, уедут все ссылки книги.

Живой контроль когорт выведен на `13_Проверки` — по строке на каждый
сценарий. Вместо сломанной сверки продаж добавлена работающая: сумма продаж
активной копии движка против листа `04_Продажи`, выбор копии через
`CHOOSE` по номеру сценария `B194`.

Финансовый эффект
: нулевой. Прогон до и после дал **пустую таблицу дельт** — ни одна цифра
не изменилась, эталон сошёлся без правок. Формул в книге стало 111 410
вместо 114 119.

Проверки
: 38 тестов зелёные (красным остаётся только `test_склад_не_отрицательный`
— см. раздел про дефицит формы). Четыре новых контроля на `13_Проверки`
показывают OK. Аудит: важных замечаний ноль, «висяков» стало 401 вместо 428.

---

## 11.09.2026 — начало регулярного пополнения склада: 01.08.2028 → 01.11.2027

Проблема
: при 1,5 школы на партнёра стартовые 50 млн товарного запаса кончались за
16 месяцев вместо заявленных в комментарии двух лет. Регулярное пополнение
при этом начиналось только с 01.08.2028 (`01_Вводные!B81`), и семь месяцев
подряд — январь–июль 2028 — сеть оставалась без формы: склад уходил в минус
на 11,7 млн по себестоимости. Тест `test_склад_не_отрицательный` падал.

Причина
: дата начала закупок была рассчитана под прежнюю скорость роста сети.
Когда коэффициент школ на партнёра вырос с 1 до 1,5, расход формы вырос
вместе с ним, а дата осталась прежней.

Решение
: `B81` = 01.11.2027 — за два месяца до первого дефицитного месяца.
Проверены также 01.05.2027 и старт модели: дефицит исчезает в любом из
вариантов, выбран самый поздний из работающих, чтобы не замораживать
деньги в складе раньше необходимого.

Финансовый эффект
:

| Показатель | Было | Стало |
|---|---:|---:|
| Дефицит формы (необеспеченный спрос) | 11 679 756 | **0** |
| Месяцев без формы | 7 | **0** |
| EBITDA | 763 929 136 | 763 929 136 |
| Деньги на конец горизонта | 476 204 153 | 476 204 153 |
| Минимальный остаток денег | −3 379 030 | −3 379 030 |
| Месяцев с отрицательной кассой | 6 | 8 |

EBITDA, деньги на конец и глубина кассового минимума не изменились. Цена
правки — два дополнительных месяца лёгкого минуса (22-й и 23-й): закупка
сдвинулась раньше, и деньги на неё уходят раньше. Ранняя оценка «не стоит
ничего по деньгам» делалась при коэффициенте 1 школа на партнёра; при 1,5
эти два месяца появляются.

Проверки
: 39 тестов зелёные — впервые вместе с `test_склад_не_отрицательный`,
который падал с 11.09.2026. Аудит без важных замечаний. V9.0 пересобрана,
сверка с V8.9 — 59 показателей до копейки. Сверка с ТЗ v6.011 — 61
требование.

---

## 11.09.2026 — порог возврата займа: развязан и выставлен в 4 месяца

Продолжение записи ниже. После замера владелец выбрал вариант 2: порог
опущен до четырёх месяцев (`B310` = 4). Займ снова гасится полностью —
50 млн за пять лет, на конец ноль. Деньги на конец 476 204 153, минимум
кассы −3 379 030, EBITDA без изменений.

Главное, что дала правка, — порог теперь **отдельная вводная**. Раньше он
неявно следовал за целевым запасом закупки, и смена одного параметра
молча меняла правило возврата долга. Теперь это видно и управляется.

---

## 11.09.2026 — порог возврата займа отвязан от целевого запаса

Проблема
: ТЗ п. 5.6 требует возвращать займ клубу, только пока на складе остаётся
запас на шесть месяцев будущих продаж. В модели этот порог читался из той
же строки, что и целевой запас закупки, поэтому после перехода на
четырёхмесячный запас (10.09.2026) правило незаметно смягчилось до четырёх
месяцев.

Причина
: две разные величины считались одной формулой. Целевой запас отвечает на
вопрос «сколько формы везти», порог возврата — на вопрос «когда можно
отдавать долг». Их совпадение было случайным.

Решение
: заведена вводная `01_Вводные!B310` — «Минимальный запас для возврата
займа», 6 месяцев по ТЗ. Отдельная строка расчёта появилась в витрине
(`07_Мерч!34`) и во всех пяти копиях движка. В движке под неё заняты
мёртвые строки, очищенные тем же днём, — новых строк не добавилось, адреса
не сдвинулись.

Заодно зафиксированы два статуса без изменения цифр: конверсия продажи в
открытие (`B298`) помечена как штатный параметр со значением 100 %, а
источником стартовых юрлиц (`B10` = 51) назван реестр сети от 21.08.2026 —
48 партнёров подписали ДКК, 3 в переговорах.

Финансовый эффект
: правка не бесплатная, и это главное, что нужно знать.

| Показатель | Порог 4 мес. | Порог 6 мес. |
|---|---:|---:|
| EBITDA | 763 929 136 | 763 929 136 |
| Погашено займа за 5 лет | 50 000 000 | **4 956 680** |
| Займ на конец горизонта | 0 | **45 043 320** |
| Деньги на конец | 476 204 153 | 521 247 473 |
| Минимум кассы | −3 379 030 | −3 379 030 |
| Месяцев с отрицательной кассой | 8 | 6 |

Займ перестал гаситься. Причина арифметическая: закупка доводит склад до
четырёхмесячного запаса, поэтому шестимесячного порога он почти никогда не
достигает, и условие возврата не срабатывает. Через пять лет 45 млн из
50 остаются долгом перед клубом, а деньги на эту же сумму лежат в компании.

Это не ошибка расчёта, а следствие буквального исполнения ТЗ при
четырёхмесячной закупке. Требует решения владельца — см. раздел ниже.

Проверки
: 38 тестов зелёные, эталон принят с новыми цифрами. Аудит без важных
замечаний. V9.0 пересобрана, 59 показателей сходятся. Сверка с ТЗ — 61
требование.

---

## 11.09.2026 — премия Евгения: 4 % это полная стоимость, а не оклад

Проблема
: модель считала 4 % от паушальных как gross-премию и начисляла сверху
страховые взносы. Компания тратила 5,2 % от паушальных вместо 4 %.

Причина
: формулировка ТЗ «4 % от суммы паушальных взносов, вычитать НДС, ФСС,
НДФЛ» допускала два прочтения. Разбор Романа 11.09.2026 снял разночтение
примером:

```
выручка от паушальных        1 000 000
минус НДС 5 %                  950 000
база премии 4 %                 38 000   ← столько тратит компания
минус ФСС: 38 000 / 1,3         29 231   ← gross, взносы уже внутри
минус НДФЛ: × 0,87              25 430   ← на руки
```

Решение
: `08_ФОТ!17` и её копии в движке считают gross как
`паушальные × 4 % / (1 + страховые взносы)`. Строка взносов не менялась —
она по-прежнему берёт 30 % от gross, и в сумме выходит ровно 4 %.
Проверено на трёх месяцах: полная стоимость премии равна 4,00 % от
паушальных без НДС.

Финансовый эффект
:

| Показатель | Было | Стало | Дельта |
|---|---:|---:|---:|
| EBITDA | 763 929 136 | 767 789 437 | +3 860 301 |
| ФОТ и кадровые расходы | 437 092 481 | 433 232 180 | −3 860 301 |
| Деньги на конец | 476 204 153 | 479 604 393 | +3 400 240 |

Экономия ровно равна взносам, которые раньше начислялись сверх премии.

Проверки
: 39 тестов зелёные, эталон принят, аудит без важных замечаний, V9.0
сходится с V8.9.

---

## Требует решения владельца

### ~~Займ клубу почти не гасится~~ — закрыт 11.09.2026: порог опущен до 4 месяцев

### Прежнее описание

После развязки порога (11.09.2026) правило ТЗ исполняется буквально, и
из-за этого возврат займа фактически остановился: 4,96 млн за пять лет из
50 млн, на конец горизонта 45 млн долга.

Механика простая: закупка доводит склад до запаса на четыре месяца вперёд,
а отдавать долг разрешено только при запасе на шесть. Склад почти никогда
не поднимается до порога.

Три выхода, любой требует решения:

1. **Закупать на шесть месяцев** — как в первом абзаце п. 5.6 ТЗ. Тогда
   порог достижим, займ гасится, но возвращается кассовый разрыв на 40 млн,
   ради устранения которого запас и снижали до четырёх.
2. **Опустить порог до четырёх месяцев** — вернуться к тому, что было до
   этой правки: займ гасится полностью, но правило мягче ТЗ.
3. **Оставить как есть** — признать, что займ гасится медленно и 45 млн
   остаются долгом перед клубом на конец пятилетки. Деньги при этом
   остаются в компании.

### Порог возврата займа съехал вместе с целевым запасом

ТЗ п. 5.6 требует: платёж по займу ограничен «требованием сохранить склад
минимум на шесть месяцев будущих продаж». В модели это правило есть —
`07_Мерч!31` разрешает погашение, только если склад не ниже целевого
запаса, а переключатель `B238` = «Да».

Но целевой запас и порог для займа — одна и та же строка `07_Мерч!24`,
которая считается по `B80`. После перехода на четырёхмесячный запас
(правка 10.09.2026) порог для возврата займа тоже стал четырёхмесячным.
По деньгам это ничего не изменило — займ и так гасится полностью, 50 млн
и до, и после, — но правило стало мягче, чем требует ТЗ.

Развязать можно отдельной вводной «минимальный запас для возврата займа»
со значением 6, оставив целевой запас закупки равным 4. Это правка формул
в `07_Мерч` и во всех копиях движка, поэтому нужно подтверждение.

### Стартовые юрлица: ТЗ говорит 49, реестр — 51

ТЗ v6.011 п. 5.3: «Стартовое количество юрлиц 49». В модели `B10` = 51,
и это опирается на `sources/Реестр_сети_21.08.2026.md`: «51 действующий
партнёр, у которых в сумме 104 действующие школы», из них 48 переподписали
ДКК и 3 в переговорах.

Реестр свежее ТЗ и разобран поимённо, поэтому модель считает по нему.
Нужно подтверждение, какая цифра правильная: если 49, меняется стартовая
база партнёров и всё, что от неё зависит.

### ~~Дефицит формы в месяцах 17–23~~ — закрыт 11.09.2026

Решено сдвигом `B81` на 01.11.2027, см. запись выше.

### Прежнее описание

Всплыл при переходе на 1,5 школы на партнёра. Регулярное пополнение склада
начинается с 01.08.2028 (`01_Вводные!B81`) — до этого сеть живёт на
стартовых 50 млн, и в комментарии написано «стартовые ~50 млн распродаются
около двух лет». При 1,5 школах они кончаются за 16 месяцев: склад уходит в
минус на 11,7 млн по себестоимости, семь месяцев подряд сеть не обеспечена
формой.

Посчитаны варианты сдвига `B81`:

| Начало регулярного пополнения | Дефицит формы | Месяцев в дефиците | Мин. касса | Деньги на конец |
|---|---:|---:|---:|---:|
| 01.08.2028 — как сейчас | 11,7 млн | 7 | −3,4 млн | 476,2 млн |
| 01.11.2027 | 0 | 0 | −3,4 млн | 476,2 млн |
| 01.05.2027 | 0 | 0 | −3,4 млн | 476,2 млн |
| со старта модели | 0 | 0 | −3,4 млн | 476,2 млн |

Сдвиг ничего не стоит по деньгам: касса и остаток на конец не меняются, а
дефицит исчезает. Рекомендация — 01.11.2027, то есть за два месяца до
первого дефицитного месяца. Требуется подтверждение владельца: это дата
закупки, а не техническая правка.

Найдено при первом прогоне, не исправлено — потому что это вопрос
экономики, а не формул.

**Чем закрывается остаточный кассовый разрыв.** После перехода на запас
в 4 месяца (правка 10.09.2026) пик сократился с 39 969 365 до
6 764 541 ₽, но минус остался в двух эпизодах: месяцы 3–11 (разгон,
до −4,2 млн) и месяцы 34–35 (−6,8 млн).

В модели он по-прежнему ничем не финансируется: строки привлечения денег
в БДДС нет, процентов нет. Фактически это беспроцентный овердрафт. Пока
он не заведён явно, EBITDA и прибыль завышены на стоимость этих денег.

Предложение: завести кредитную линию около 10 млн со ставкой отдельной
строкой БДДС. Правка затрагивает формулы и должна вноситься в два места —
витрину `02_Модель` и три сценарные копии в `19_Расчет_аналитики`.

---

## 11.09.2026 — защита ввода: выпадающие списки и запрет неверных форматов

Проблема
: `01_Вводные` принимал что угодно. В ячейку доли можно было вписать «6,5»
вместо 0,065 — модель не ругалась, а тихо считала роялти в сто раз больше.
Сценарий `B192` хранит текст, и число в нём молча проваливается в
позитивную ветку (`B194`). Даты, месяцы закупок, признаки «Да / Нет» —
всё это вводилось руками без единой подсказки.

Причина
: проверок данных на листе вводных почти не было — 70 ячеек из 177 были
закрыты, остальные 107 открыты настежь. Правильный формат держался только
в голове у того, кто модель собирал.

Решение
: `tools/build_user_friendly.py` — функция `защитить_ввод()`. Тип проверки
выводится из единицы измерения в колонке C:

| единица | что разрешено | сообщение при ошибке |
|---|---|---|
| «Да / Нет» | список `Да,Нет` | — |
| сценарий `B192` | список `Негативный,Базовый,Позитивный` | подстановка номера даёт позитивный сценарий |
| `%…` | дробь 0…1 | «Вводите долей: 6,5 % это 0,065» |
| «дата» | дата 2020…2040 | — |
| «номер месяца» | целое 1…12 | — |
| «мес.», «дней» | целое 0…120 | — |
| «шт.», «чел.», «окладов», «компл./ребёнок/сезон» | число 0…100 000 | — |
| `₽…` | число ≥ 0 | — |

Закрыто 107 ячеек: дата 13, деньги ≥ 0 — 30, доля 0…1 — 27, месяц 1…12 —
10, список «Да / Нет» — 6, список сценариев 1, целое 0…120 — 9, число ≥ 0
— 11. Вместе с теми, что были, на листе 177 защищённых ячеек и 36 правил.
`tools/build_v9.py` (функция `копировать_лист`) переносит проверки в
V9.0 — раньше они терялись при копировании листа.

Финансовый эффект
: нулевой по построению. Проверка данных живёт в оформлении листа и ни в
одну формулу не входит. Прогон «база» после правки: EBITDA 767 789 437,
деньги на конец 479 584 943, минимум −3 212 370, дети 3 835, школы 429 —
эталон `tests/baseline.json` совпал до копейки без `--update`.

Проверки
: 39 тестов зелёные, аудит 1 023 находки и 0 важных (как до правки),
`compare_v8_v9.py` — «экономика перенесена без изменений» (59 из 59),
`check_tz.py` — 61 совпадение, как и было. Сама защита проверяется тем,
что обе книги пересчитываются LibreOffice: правила корректны, иначе файл
не открылся бы.

---

## 11.09.2026 — навигация: где вводится дата старта

Проблема
: в формуле видно имя `СтартМодели`, а где оно вводится — нет. Владелец
модели искал ячейку глазами среди 300 вводных и не нашёл.

Причина
: имена вводных сделали формулы читаемыми, но убрали адрес. Подпись
«Дата старта модели» есть в `01_Вводные!A7`, однако до неё надо
доскроллить, а инструкция про адреса ввода ничего не говорила.

Решение
: `подсказки_вводных()` в `tools/build_user_friendly.py` вешает записку на
ячейку — что это, откуда берётся имя, какие значения допустимы. Закрыты
`B7` (дата старта), `B8` (горизонт), `B9`/`B10`, `B13`, `B22`, `B192`;
там, где записка уже была, она сохранена. В `09_Инструкция` V9.0 добавлен
раздел «Где что вводится» с адресами четырёх самых нужных ячеек.

Финансовый эффект
: нулевой. Записка живёт в оформлении ячейки, в расчёт не входит. Прогон
после правки: EBITDA 767 789 437, деньги на конец 479 584 943, минимум
−3 212 370 — эталон совпал без `--update`.

Проверки
: 39 тестов, `compare_v8_v9.py` — 59 из 59 без расхождений.

---

## 11.09.2026 — читаемость V9.0: разряды, даты месяцев, честный итог

Проблема
: три вещи, из-за которых в книгу было неприятно смотреть.
(1) Числа шли без разделителя разрядов, а количество школ рисовалось как
«06» вместо «6,2». (2) Столбцы месяцев подписаны `М1…М60` — какой это
календарный месяц, приходилось считать в уме. (3) В строках-остатках
(когорты школ, остаток денег) колонки лет показывают снимок последнего
месяца, а шапка над ними обещает «Итого 5 лет»; у когортных строк это
вдобавок бессмысленно — одна школа попала бы в годовую сумму двенадцать
раз.

Причина
: форматы в `tools/v9_lib.py` были записаны русскими символами —
`'# ##0,0'`. Excel хранит коды форматов только в канонической записи
(запятая — разряды, точка — десятичная), поэтому `,0` он прочитал как
деление на тысячу. Шапка была однострочной, а `ряд()` не различал строки
потоков и строки остатков.

Решение
: `ДЕНЬГИ`, `ШТУКИ`, `ЦЕЛЫЕ`, `ПРОЦЕНТ`, `ДАТА` переписаны каноническими
кодами: `#,##0`, `#,##0.0`, `0.0%`, `mmm yyyy`. Разделитель разрядов — у
денег, штук и людей; у процентов, коэффициентов и индексов его нет. Люди
считаются целыми. `шапка()` пишет две строки: `М1…М60` и под ними
настоящую дату месяца формулой от `СтартМодели` — подпись не зависит от
языка Excel. В `ряд()` появился параметр `годы`: у 25 когортных строк
годовые колонки пусты, а в «Как считается» сказано, что это внутренняя
механика; у остальных строк-остатков туда же добавлено «в колонках лет —
значение на конец года, это остаток, а не сумма».

Заодно `tools/compare_v8_v9.py` переведён с номеров строк V9.0 на названия:
новая строка шапки сдвинула лист на единицу, и сверка показала 59
расхождений там, где экономика цела. Теперь сдвиг ей безразличен.

Финансовый эффект
: нулевой. Формат ячейки, текст подписи и пустая колонка в расчёт не
входят. `compare_v8_v9.py` — 59 из 59 без расхождений, эталон не
пересчитывался.

Проверки
: `compare_v8_v9.py` (59 показателей помесячно), 39 тестов на V8.9 — она
этой правкой не затронута.

---

## 11.09.2026 — роялти действующей сети: котёл → группы точек начисления

Проблема
: `06_Сеть_и_дети!28` и пять её копий в движке считали роялти действующей
сети котлом: `MAX(6,5 % × общий оборот; число школ × 15 000)`. Котёл
ошибается дважды. Он не видит договорных исключений — пять юрлиц платят
3,25 %, одно платит только фикс без процента. И он берёт фикс с каждой из
104 школ, хотя счёт выставляется на точку начисления, а их 48: у одних
партнёров это юрлицо целиком, у других — каждый филиал отдельно (у
Кирюшина шесть адресов, на каждом свой фикс).

Причина
: пошкольного расчёта не было, потому что не было данных. 11.09.2026
владелец модели передал реестр начислений и описал механику словами:
«либо фикс на все филиалы, либо за каждый филиал свой фикс; если
начисленный по выручке роялти превышает фикс — включается процент».

Решение
: раздел 16 листа вводных — таблица из пяти групп точек начисления,
снятая с факта августа 2026 (81 точка у 65 юрлиц, из них 48 живых):

| группа | точек | доля выручки | средний фикс | ставка |
|---|---:|---:|---:|---:|
| Крупные — платят процент | 10 | 55,56 % | 13 560 | 6,50 % |
| Льготная ставка | 5 | 29,61 % | 9 000 | 3,25 % |
| Средние — у порога фикса | 16 | 9,13 % | 19 138 | 6,50 % |
| Мелкие — платят фикс | 15 | 3,31 % | 20 067 | 6,50 % |
| Только фикс, без процента | 2 | 2,38 % | 20 000 | 0 % |

Внутри группы `MAX(фикс × точек; ставка × выручка группы)`, наружу —
сумма. `MAX` внутри `SUMPRODUCT` поэлементно не работает, поэтому взято
тождество `MAX(a,b) = (a + b + |a − b|) / 2` — чистая арифметика,
считается одинаково в Excel, LibreOffice и Google Sheets. Число точек
убывает вместе со школами; ставка группы переключается на общую `B57`
после даты перехода `B245` — ровно как делал котёл. `B52` из ручного
минимума стал справочным средним фиксом на точку и в расчёт не входит.

Профиль воспроизводит факт августа 2026: 2 698 475 ₽ против фактических
2 701 108 ₽ (0,1 %), сумма фиксов 827 813 против 827 800. На росте
выручки от 1× до 2,5× группы повторяют поточечный расчёт с ошибкой
0,1–0,8 %, котёл ошибался на 8–18 %.

Финансовый эффект
: роялти действующей сети за 60 месяцев 155,05 → 149,83 млн ₽ с НДС
(−5,23 млн). Знак разный по месяцам: в месяцы полной загрузки группы
дают больше котла (сентябрь 2026: 1,78 против 1,61 млн — котёл занижал
фикс), а летом меньше (август 2027: 0,80 против 1,51 млн — котёл брал
фикс со 104 школ вместо 48 точек).

| показатель | было | стало | дельта |
|---|---:|---:|---:|
| Выручка без НДС | 2 241 351 135 | 2 236 378 189 | −4 972 947 |
| EBITDA | 767 789 437 | 763 313 785 | −4 475 652 |
| Прибыль после УСН | 647 944 491 | 643 942 954 | −4 001 537 |
| Деньги на конец | 479 584 943 | 475 853 423 | −3 731 520 |
| Минимальный остаток | −3 212 370 | −2 955 926 | +256 444 |
| Выплаты ФК — роялти | 99 401 314 | 98 904 019 | −497 295 |

Проверки
: новый файл `tests/test_роялти_действующей_сети.py` — три теста: роялти
месяца не ниже суммы фиксов живых точек; фикс берётся с точки начисления,
а не со школы (минимум за горизонт должен быть заметно ниже уровня
«104 × 15 000»); доли выручки групп дают ровно единицу. В `selfcheck.py`
добавлен дефект «котёл»: возврат старой формулы во все 60 месяцев —
тесты его ловят. Заодно усилен `test_на_партнёра_приходится_больше_одной_школы`:
он смотрел максимум по всем месяцам, а новые школы вытягивали его сами,
поэтому подмену стартовой сети он пропускал; теперь проверяется первый
месяц. Всего 42 теста, аудит 0 важных, `compare_v8_v9.py` 59 из 59,
`check_tz.py` — 61 совпадение, как и было.

Осталось открытым
: после даты общей ставки (`B245`, 01.01.2030) на 10 % переходят все
группы, включая льготные и ту, что платит только фикс. Так требует ТЗ 5.4
и так делал котёл, но договорные исключения в жизни могут пережить
переход. Решает владелец модели.

---

## 11.09.2026 — партнёр открывает школы не разом

Проблема
: `06_Сеть_и_дети!8` и пять её копий в движке умножали продажи на
коэффициент школ на партнёра (`B22` = 1,5) целиком в месяц открытия.
Каждая продажа сразу давала полторы школы — как будто партнёр запускает
две площадки одновременно. Владелец модели: «коэффициент не должен
применяться сразу — не все партнёры сразу открывают по 2 школы».

Причина
: `B22` появился 11.09.2026 как простой множитель. При значении 1 разницы
не было, и вопрос «когда именно открывается вторая школа» не вставал.

Решение
: формула открытий разложена на два слагаемых. Первая школа —
через `B15` (лаг продажа → открытие), остальные (`B22` минус одна) — ещё
через `B311`, новую вводную «Лаг открытия следующих школ партнёра».
`MIN(1;B22)` и `MAX(0;B22−1)` держат формулу осмысленной при любом
значении: меньше единицы — второй площадки просто нет, ноль в `B311` —
возврат к прежнему поведению. Функция `link_schools_per_partner()`,
которая пробрасывала `B22` в старую формулу, удалена: новая пишет
формулу целиком, включая технический хвост за горизонтом.

По умолчанию `B311` = 3 месяца. Это не догадка, а нижняя граница по
факту: в реестре на 21.08.2026 65 юрлиц держат 121 школу, из них 36 —
ровно одну; пять случаев второй школы с известными датами договоров дают
разрыв 0–3 месяца. Окно наблюдения — один сезон, поэтому наблюдение
смещено в короткую сторону.

Финансовый эффект
: при `B311` = 3 EBITDA 763,31 → 731,49 млн ₽ (−31,83 млн). Параметр
решает многое, поэтому вот вся чувствительность:

| `B311` | EBITDA | Выручка | Школ на конец | Мин. касса |
|---:|---:|---:|---:|---:|
| 0 (как было) | 763 313 785 | 2 236 378 189 | 429 | −2 955 926 |
| 3 | 731 487 728 | 2 181 352 193 | 425 | −8 707 185 |
| 6 | 699 638 539 | 2 127 951 010 | 420 | −13 848 391 |
| 12 | 638 013 264 | 2 021 735 102 | 407 | −16 352 076 |
| 24 | 540 799 051 | 1 855 080 161 | 379 | −15 521 779 |

Школ на конец падает потому, что вторые школы последних продаж уезжают
за горизонт; детей — сильнее, потому что школы моложе и не успевают
дойти до зрелой ёмкости. Кассовый разрыв углубляется с −2,96 до −8,71 млн:
расходы на привлечение идут по плану продаж, а роялти приходит позже.

Проверки
: новый файл `tests/test_открытие_школ.py` — в месяц первого открытия на
продажу приходится ровно одна школа; школы сверх первой не потеряны, а
сдвинуты (сумма открытий между «одна на продажу» и «`B22` на продажу»).
В `selfcheck.py` дефект «разом»: формула открытий возвращается к старому
виду — тесты его ловят. В срез прогона добавлена вводная `B311`, чтобы
тесты видели режим. Всего 44 теста, аудит 0 важных, `compare_v8_v9.py`
59 из 59.

---

## 11.09.2026 — история: сезон 25/26 включается переключателем

Проблема
: ТЗ v6.011 п.10 просит видеть итог в двух вариациях — с 01.09.2025 и с
01.09.2026. Модель умела только вторую: горизонт начинается с даты старта,
всё, что было раньше, в ней не существует.

Причина
: сезон 25/26 — это факт, а не прогноз. Считать его формулами нельзя,
а места, куда положить его итоги, в книге не было.

Решение
: раздел 17 листа вводных — переключатель `B322` «Учитывать сезон 25/26»
(Да / Нет, по умолчанию Нет) и восемь итогов сезона: выручка, EBITDA,
прибыль, поток, деньги на конец, школы, партнёры, дети. На дашборде V9.0
появилась колонка «Сезон 25/26 (факт)» слева от первого года и колонка
«С сезоном 25/26» справа от пятилетнего итога. Потоки складываются,
остатки и число школ — нет: это снимок последнего месяца прогноза, а не
сумма. При выключенной истории колонка факта пуста, итог с историей равен
итогу за пять лет.

Заполнено то, что подтверждено фактом: школы на конец сезона 104 и
партнёры 51 по реестру сети на 21.08.2026; отдельной справочной строкой —
начисленное за сезон роялти 24 779 013 ₽ с НДС по реестру начислений.
Остальные строки стоят нулями со статусом «заполнить из факта»:
в переданных файлах P&L управляющей компании есть только за июнь и июль
2026, а сентябрь 2025 отсутствует и в реестре начислений.

Чтобы включённая история с нулями молча не занижала итог, на
`08_Проверки` добавлен седьмой контроль: «История: если включена, факт
сезона 25/26 заполнен». Он считает нули в разделе 17 и при включённом
переключателе показывает ПРОВЕРИТЬ.

Заодно исправлена ошибка, которую внесла двухстрочная шапка: дашборд
брал строки сети с `02_Результат` по номерам 6/7/8, а после сдвига это
стали пустая строка, «Школы всего» и «Партнёры». Школ на дашборде
показывалось 0, а в строке детей стояли партнёры. Теперь строки ищутся по
названию — как и в `compare_v8_v9.py`.

Финансовый эффект
: на прогноз нулевой — переключатель выключен, и сам расчёт истории не
касается. Прогон после правки совпал с эталоном без `--update`: EBITDA
731 487 728, деньги на конец 451 069 418. Исправление строк дашборда
цифры не двигает, а показывает верные: школ на конец пятого года 515
вместо нуля.

Проверки
: 44 теста, `compare_v8_v9.py` 59 из 59. Механика проверена прогоном V9.0
с включённой историей и подставленными числами: колонка факта появляется,
итог за шесть сезонов складывается, контроль на `08_Проверки` показывает
ПРОВЕРИТЬ на четырёх незаполненных строках.

---

## 11.09.2026 — модель считается от любой даты старта

Проблема
: `01_Вводные!B7` принимал только 1 сентября, и не зря: две вещи были
привязаны не к календарю, а к номеру столбца, и при другом старте молча
считали не то.

1. Веса плана продаж (`01_Вводные!C36:N40`, подписаны Сен…Авг) брались по
   номеру месяца в сезоне. При старте в декабре декабрь получал
   сентябрьские 18 %, а сентябрь — декабрьские 2 %.
2. Согласованный мерч действующей сети (`01_Вводные!B99:E101`, факт за
   сентябрь–декабрь 2026) стоял в первых четырёх столбцах `07_Мерч`. При
   старте в декабре первый месяц забирал сентябрьские 5 070 комплектов.

Причина
: обе таблицы подписаны месяцами, но читались по позиции. Пока старт был
сентябрём, позиция и календарь совпадали, и расхождение не проявлялось —
тот же случай, что с `B22` при значении 1.

Решение
: `развязать_от_сентября()` в `tools/build_user_friendly.py`.

Веса ищутся формулой `INDEX(веса; MOD(MONTH(дата)−9;12)+1)` — «сколько
месяцев прошло от сентября». Правило «первый месяц модели без продаж»
переехало из данных в формулу: раньше оно было зашито в строку 36, где
сентябрь стоял нулём, а остальные веса заранее поделены на 0,82. Теперь
строка 36 — обычный профиль года, такой же как у сезонов 2–5, а вес
месяца старта вычитается из нормировки первого сезона. Годовой план
сезона при этом выполняется целиком при любом старте.

Согласованный мерч ищется по дате: подписи `B98:E98` стали настоящими
датами, а `07_Мерч` строки 5–7 во всех 66 столбцах спрашивают
`COUNTIF/SUMIF` — есть ли этот месяц в согласованной таблице. Контроль
`13_Проверки!60` сравнивает только те согласованные месяцы, что попали в
горизонт: при старте позже сентября часть их остаётся за его пределами, и
раньше контроль падал на ровном месте.

Замок на `B7` снят: остаётся требование первого числа — месяц это
минимальный шаг модели.

Заодно починен `tools/run_model.py`: `--set` клал дату строкой, а не
датой. LibreOffice текст в дату приводит, но не везде одинаково, и
сентябрьский прогон через `--set` расходился с обычным на 1 000 ₽.

Финансовый эффект
: при старте 1 сентября — ровно нулевой, и это главная проверка правки.
EBITDA 731 487 728, деньги на конец 451 069 418, минимальный остаток
−8 707 185 — эталон совпал без `--update`. При других стартах цифры,
разумеется, другие: первый месяц без продаж теряет вес месяца старта, и
сентябрьский старт жертвует самым сильным месяцем года (18 %), а
декабрьский — самым слабым (2 %).

| старт | Выручка | EBITDA | Мин. касса |
|---|---:|---:|---:|
| 01.09.2026 | 2 181 352 193 | 731 487 728 | −8 707 185 |
| 01.12.2026 | 2 414 181 503 | 809 930 416 | −10 595 917 |
| 01.03.2027 | 2 412 340 510 | 839 329 362 | −12 427 219 |
| 01.06.2027 | 2 433 365 936 | 877 341 689 | −16 110 108 |

Проверки
: новый файл `tests/test_дата_старта.py` — пять тестов на декабрьском
прогоне: нет ошибок в ячейках, не падают собственные проверки модели,
план каждого сезона выполняется целиком, пик продаж остаётся сентябрём, а
не съезжает на первый месяц сезона, и согласованный мерч не съезжает на
первый столбец. В `selfcheck.py` дефект «календарь»: веса снова берутся
по номеру месяца — ловится.

Чтобы дефект вообще доезжал до таких тестов, `conftest.ensure_run()`
научился переменной `SPARTAK_MODEL`, а `selfcheck.py` её выставляет:
раньше производный прогон считался с целой книги, и подсаженная поломка
до него не доходила. Всего 49 тестов, аудит 0 важных, `compare_v8_v9.py`
59 из 59, `check_tz.py` — 61 совпадение.

---

## 11.09.2026 — графики на дашборде и печать

Проблема
: дашборд показывал только числа. Понять, где кассовый разрыв, когда
гасится займ и почему выручка скачет, по таблице из пяти колонок нельзя —
всё это видно только помесячно. Плюс подзаголовок печатал «старт ММ.ГГГГ»
вместо даты, а на печать уезжали только две первые колонки.

Причина
: графиков в сборщике не было. Подзаголовок использовал русский код
формата внутри `TEXT()` — та же ошибка, что была в форматах ячеек: Excel
и LibreOffice понимают только каноническую запись. Настройки печати у
V9.0 не было вовсе.

Решение
: `построить_графики()` в `tools/build_v9.py` — восемь помесячных
графиков в два столбца под таблицей:

| график | что показывает |
|---|---|
| Сеть: школы и дети | школы и дети на второй оси |
| Продажи франшиз и открытия школ | столбцы: продано и открыто |
| Выручка и EBITDA | две линии |
| Роялти: откуда он берётся | действующая сеть против новых школ |
| Форма: выручка и прибыль | столбцы с прибылью до доли клуба |
| Займ клубу | погашение столбцами, остаток долга линией |
| Деньги на конец месяца | тот самый кассовый разрыв |
| Склад формы | склад против целевого запаса |

Строки для графиков ищутся по названию в колонке B — новая строка на
листе график не ломает. Подписи оси берутся из строки дат в шапке, то
есть едут вместе с датой старта.

`TEXT(СтартМодели,"ММ.ГГГГ")` исправлен на `"MM.YYYY"`, `TEXT($B$22,"0,0")`
— на `"0.0"`. `настроить_печать()` ставит альбомную ориентацию, вписывание
по ширине и повтор шапки на каждой странице; у дашборда область печати —
таблица с итогами, графики остаются для экрана, чтобы итог помещался на
один лист.

Финансовый эффект
: нулевой. График и настройка печати в расчёт не входят, эталон совпал
без `--update`.

Проверки
: 49 тестов, `compare_v8_v9.py` 59 из 59. Книга пересчитана LibreOffice —
все восемь графиков переживают пересчёт с обоими рядами. Дашборд
выгружен в PDF и просмотрен глазами: данные на графиках есть, подписи оси
календарные, таблица помещается на страницу целиком.

---

## 11.09.2026 — сезон 25/26 фактом: лист `10_История` и P&L на дашборде

Проблема
: дашборд показывал из БДР только выручку, EBITDA и прибыль. Где именно
расходится результат — в прямых расходах, в ФОТ или в прочем OPEX — по
нему было не понять. Сезон 25/26 при этом стоял нулями со статусом
«заполнить», хотя данные за него есть.

Причина
: колонка истории собиралась под три строки БДР, а факт сезона никто не
разложил по статьям: в переданных файлах он лежит в двух разных
форматах — помесячный реестр начислений и P&L за два месяца.

Решение
: раздел 17 вводных развёрнут до полного БДР — шестнадцать строк в тех же
статьях, что и `02_Результат`, плюс два блока факта: реестр начислений за
одиннадцать месяцев (оборот сети и роялти помесячно) и P&L управляющей
компании за июнь и июль 2026, разложенный по статьям модели
(«коммерческие расходы» отчёта — это выплаты клубу, «административные»
делятся на ФОТ со взносами и прочий OPEX).

Итоги сезона посчитаны как средний месяц P&L на одиннадцать и помечены
«Оценка: 2 мес. P&L x 11» — построчно, чтобы это нельзя было принять за
факт. Строки, известные точно, помечены источником: школы 104 и партнёры
51 по реестру сети, дети 3 808 (проданных абонементов в августе), деньги
на конец 187 878 (ДДС на 02.07.2026). Денежный поток за сезон оставлен
нулём со статусом «нет данных»: ДДС передан только за первое полугодие и
в PDF. Контроль на `08_Проверки` из-за этого показывает ПРОВЕРИТЬ при
включённой истории — и правильно делает.

Новый лист `10_История` раскладывает всё это в виде отчёта о финансовых
результатах: статьи сезона со статусом и комментарием у каждой строки,
сеть на конец сезона, помесячный реестр с эффективной ставкой роялти и
P&L двух известных месяцев со средним. Лист ничего не считает — только
ссылается на раздел 17, поэтому правило «все вводные на 01_Вводные»
осталось целым.

Дашборд получил укрупнённый P&L вместо трёх строк: паушальные, роялти,
мерч, выручка, прямые расходы, валовая прибыль, ФОТ, прочий OPEX, EBITDA,
УСН, прибыль после налога. Ради этого на `02_Результат` добавлена строка
«OPEX — прочие (без ФОТ)»: `OPEX — всего` включает ФОТ, и без такой
строки ФОТ и OPEX на дашборде складывались бы глазами в двойной счёт.
Область печати дашборда теперь считается от последней строки таблицы, а
не зашита числом — иначе после добавления строк сеть уехала бы за край,
как и уехала при первой сборке.

Финансовый эффект
: на прогноз нулевой — история выключена, а `OPEX — прочие` это
промежуточный итог, который ни во что не входит. Эталон совпал без
`--update`: EBITDA 731 487 728, деньги на конец 451 069 418.

Что даёт включённая история (`B322` = «Да»):

| | сезон 25/26 | 5 лет прогноза | вместе |
|---|---:|---:|---:|
| Выручка без НДС | 37 839 918 | 2 181 352 193 | 2 219 192 111 |
| EBITDA | −27 569 953 | 731 487 728 | 703 917 775 |
| Прибыль после налога | −27 569 953 | 616 435 666 | 588 865 713 |

Проверки
: 49 тестов, `compare_v8_v9.py` 59 из 59, аудит 0 важных. Книга
выгружена в PDF и просмотрена глазами: дашборд с полным P&L помещается на
страницу, лист истории читается, эффективная ставка роялти по реестру
даёт 5,9–7,8 % по месяцам и 6,5 % за одиннадцать месяцев.

---

## 11.09.2026 — сезон 25/26: оценка заменена фактом

Проблема
: итоги сезона 25/26 были посчитаны как средний месяц P&L на одиннадцать
— другого способа не было, отчёт существовал только за июнь и июль 2026.
Оценка оказалась сильно завышенной.

Причина
: июнь и июль — не средние месяцы сезона. Сентябрь и октябрь 2025 вообще
без выручки (сеть только запускалась), а роялти росло весь сезон. Средний
месяц по двум поздним месяцам дал выручку 37,8 млн вместо фактических
26,4 млн — на 43 % больше.

Решение
: владелец модели передал отчёт о финансовых результатах за август 2025 —
май 2026 помесячно. Вместе с отдельными отчётами за июнь и июль 2026 это
закрывает одиннадцать месяцев сезона — сентябрь 2025 — июль 2026 —
фактом. Август 2025 в отчёте есть, но относится к прошлому сезону и в
итог не берётся; август 2026 в отчётность ещё не попал.

Раздел 17 вводных перестроен: помесячная таблица P&L на 11 месяцев в
статьях модели (паушальные, роялти, мерч, себестоимость формы, выплаты
клубу, ФОТ со взносами, прочий OPEX, прочие расходы, налог), а итоги
сезона стали **формулами** по ней. Добавится август 2026 — итог
пересчитается сам, руками ничего править не надо. Лист `10_История`
показывает ту же таблицу помесячно.

Финансовый эффект
: на прогноз нулевой — история выключена по умолчанию. Сам сезон
изменился так:

| | было (оценка) | стало (факт) |
|---|---:|---:|
| Выручка без НДС | 37 839 918 | 26 442 716 |
| Роялти | 30 372 832 | 22 016 394 |
| Мерч | 7 467 086 | 4 363 400 |
| ФОТ и кадровые | 44 964 860 | 31 027 973 |
| EBITDA | −27 569 953 | −23 683 529 |
| Прибыль после налога | −27 569 953 | −23 735 772 |

Что видно из факта и чего раньше видно не было:

- **ФОТ растёт весь сезон** — 1,57 млн в сентябре 2025, 4,28 млн в июле
  2026. За одиннадцать месяцев 31,0 млн, и он один больше всей выручки
  сезона.
- **Паушальные почти нулевые** — 62 922 ₽ за сезон: франшизы отдавались
  за символические суммы, пока сеть набиралась.
- **Мерч продаётся рывками** — февраль 1,47 млн, июль 67 тыс.
- **Реестр начислений и P&L не сходятся по роялти**: за одни и те же
  девять месяцев реестр даёт 20,36 млн, отчёт — 22,02 млн (+8 %). Базы
  разные: реестр считает начисление по школам, отчёт — выручку
  управляющей компании. Оба ряда лежат рядом на листе истории.

Сверка первого года прогноза с фактическим сезоном — то, ради чего
история и заводилась:

| статья | сезон 25/26 | модель, год 1 | во сколько раз |
|---|---:|---:|---:|
| Роялти | 22 016 394 | 24 543 943 | 1,1 |
| Паушальные | 62 922 | 60 354 271 | 959 |
| Мерч | 4 363 400 | 40 078 610 | 9,2 |
| ФОТ | 31 027 973 | 57 613 374 | 1,9 |
| EBITDA | −23 683 529 | −9 337 543 | — |

Роялти модель берёт почти точно. Паушальные и мерч — это ставка плана: 66
продаж по полной цене против семи символических и выход мерча на оборот.
ФОТ модели вдвое больше сезонного, но фактический ФОТ на выходе из сезона
— 4,28 млн в месяц, то есть 51 млн в год: модель близка к этому темпу.

Проверки
: 49 тестов, `compare_v8_v9.py` 59 из 59, эталон совпал без `--update`.
Сходимость факта проверена трижды: выручка по статьям равна строке 01
отчёта, EBITDA по своду равна строке 12, чистая прибыль — строке 11
(расхождение 1–2 ₽ на округлении).

---

## 11.09.2026 — графики по годам с цифрами, период в шапке, дата старта

Проблема
: четыре вещи, из-за которых дашборд читался с трудом.
(1) Графики шли по 60 месяцев без подписей — что за цифра под точкой,
понять было нельзя. (2) В подзаголовке печаталось «старт MM.YYYY» вместо
даты. (3) Колонка «Год 1» ничего не говорила о том, какие это месяцы, а
строка «Месяц» показывала в ней «авг 2027» — и первый год читался как
август 2027, хотя он сентябрь 2026 — август 2027. (4) Переключателя
истории на дашборде не было, и колонка сезона 25/26 выглядела необъяснимо.

Причина
: графики строились помесячно, потому что месяцы — «родной» шаг модели;
для руководителя родной шаг — год. `TEXT(дата;"MM.YYYY")` разбирается
языком самого Excel: русский понимает «ММ.ГГГГ», английский «MM.YYYY», и
любой вариант в чужой раскладке печатается как есть — это не тот случай,
что с форматом ячейки, который хранится канонически. Период года нигде не
был написан, а строка «Месяц» получала годовую колонку как снимок конца
периода.

Решение
: шесть графиков переведены на годы, значение подписано на каждой точке;
по месяцам остались два — кассовый разрыв и склад, они живут внутри года
и годовым срезом не видны. Деньги на подписях в миллионах: «862 млн»
вместо «861 752 109».

Чтобы миллионы работали везде, значения для подписей берутся из
служебного блока на самом дашборде — те же годовые цифры, поделённые на
миллион и с форматом «862 млн». Формат подписи, записанный в XML
графика, читает Excel, но не читает LibreOffice; формат исходной ячейки
читают оба, и Google Sheets тоже.

Дата в подзаголовке собирается из чисел:
`TEXT(MONTH(старт);"00")&"."&YEAR(старт)`. «00» — цифровой placeholder, он
одинаков в любой раскладке. Тем же способом исправлен `TEXT($B$13;"0,0%")`
в подписи закрытия школ на `01_Вводные`.

В шапке каждого листа под «Год N» появился период — «09.2026 – 08.2027»,
формулой от даты старта, поэтому он едет вместе с ней. Тот же период
стоит в шапке дашборда, и колонка истории подписана «Сезон 25/26 /
09.2025 – 07.2026». Календарные строки («Месяц», «Год модели», «Месяц
сезона») годовых колонок больше не получают: год — это период, а не одно
значение.

На дашборде справа от заголовка — состояние переключателя истории,
ссылка, по которой Excel переходит прямо в `01_Вводные!B322`, и строка,
объясняющая, что именно показывает колонка сезона. Ввод остался на листе
вводных: это правило книги.

Финансовый эффект
: нулевой. Подписи, шапки и служебный блок в расчёт не входят; служебный
блок лежит на строках 200+ дашборда, за пределами области печати. Эталон
совпал без `--update`.

Проверки
: 49 тестов, `compare_v8_v9.py` 59 из 59, прогон V9.0 без ошибок в
ячейках и без проваленных контролей в обоих положениях переключателя.
Дашборд выгружен в PDF и просмотрен глазами: подписи читаются в
миллионах, периоды под годами на месте, дата старта печатается как
09.2026.

---

## 11.09.2026 — лист вводных: цвета, дубль истории, выключенные блоки

Проблема
: четыре вопроса владельца модели, и все четыре — про то, что лист
вводных врёт глазу.

1. `#ЗНАЧ!` в `E22`. Подпись «Новый партнёр открывает 1,5 школы» собиралась
   через `TEXT($B$22;"0.0")`.
2. Легенда из четырёх почти одинаковых жёлтых: `FFF299` (ввод), `FFF2A5`
   («уточнить»), `FFF2A0` («переключатель»), `FFF2CC` («блок ревизии»).
   Различить их на экране нельзя, и «уточнить» читалось как обычный ввод.
3. Блок лагерей залит жёлтым как живой ввод, хотя лагеря выключены
   (`B241` = «Нет») и все его строки входят в модель нулями.
4. Два блока истории сезона 25/26: раздел 10 из исходной книги и раздел
   17, заведённый 11.09.2026. Плюс два переключателя истории — `B188` и
   `B322`.

Причина
: (1) код формата внутри `TEXT()` разбирается языком самого Excel,
включая десятичный разделитель: «0,0» работает в русском и падает в
английском, «0.0» — наоборот. Это не тот случай, что с форматом ячейки,
который хранится канонически. Я поменял русский код на английский,
починив LibreOffice и сломав Excel владельца.
(2) оттенки развелись исторически, никто их не сверял рядом.
(3) заливка ставилась по «это вводная», а не по «это влияет на расчёт».
(4) раздел 10 в исходной книге был, но на него не ссылается ни одна
формула — он лежал в столе, и при заведении раздела 17 я его не заметил.

Решение
: дробные подписи собираются арифметикой, а не форматом:
`"Закрывается "&ROUND($B$13*100;1)&" %"` и `&ROUND($B$22;1)&`. Число
форматирует сам Excel по своей локали — это как раз то, что нужно.
Безопасны внутри `TEXT()` только целые placeholder'ы вроде «00», ими
собираются даты.

Легенда разведена по цвету, а не по оттенку одного цвета: ввод жёлтый
(`FFF299`), «уточнить» светло-оранжевый (`FCD5B4`), переключатель голубой
(`D9E1F2`), блок ревизии сиреневый (`E4DFEC`). Перекрашено 256 ячеек,
строка легенды переписана короткими подписями, чтобы влезали в колонки.

Семнадцать вводных лагерей залиты серым и получили статус «Не в расчёте»,
заголовок раздела стал «9. Лагеря — блок ВЫКЛЮЧЕН (B241 = «Нет»), в
расчёт не входит». Сами значения сохранены: лагеря включаются одним
переключателем, когда владелец решит.

Раздел 10 сведён с разделом 17. Его помесячный денежный поток (январь —
июль 2026) перенесён в таблицу раздела 17 отдельной колонкой и закрыл
последнюю пустую строку сезона: −113 596 ₽ за семь доступных месяцев.
Данные дубля очищены (93 ячейки), `B188` стал формулой
`=IF(B322="Да";"С историей";"Без историей")` — выключатель истории теперь
один, а лист `00_Старт`, который читает `B188`, продолжает работать.
Контроль `13_Проверки!62`, сверявший июльский P&L по ячейкам раздела 10,
перенаправлен на раздел 17 — он по-прежнему ловит подмену факта июля.

Раздел 15 переименован из «Уточнения после ревизии 11.09.2026» в
«Возврат займа и открытие школ партнёра»: заголовок должен говорить, что
внутри, а не когда его правили.

Финансовый эффект
: нулевой. Цвет, подпись и очищенный блок, на который никто не
ссылается, в расчёт не входят. Прогон после правки: EBITDA 731 487 728,
деньги на конец 451 069 418, минимум −8 707 185 — эталон совпал без
`--update`.

Проверки
: 49 тестов, аудит 1 019 находок и 0 важных, `compare_v8_v9.py` 59 из 59,
собственные контроли модели проходят — включая перенаправленный контроль
июля. Прогон V9.0 с включённой историей: колонка сезона показывает
денежный поток −113 596 вместо нуля.

Справка по вопросам
: БИБОСС, TopFranchise и Forbes (`B28:B33`) — ежегодные размещения из
исходной книги V8.8, не что-то новое. Они входят в маркетинг привлечения
через `04_Продажи!22` и на расчёт влияют. Лагеря тоже из исходника, но
сейчас выключены и дают нули по всем шести строкам `02_Модель`.

---

## 11.09.2026 — навигация по листу вводных и нумерация разделов

Проблема
: владелец модели спросил про синие подписи в шапке `01_Вводные` —
ведут ли они куда-нибудь. Шесть исходных ссылок оказались настоящими и
рабочими, но проверка вскрыла три дефекта, два из которых внёс я.

1. Два раздела с номером «2»: исходный «2. Продажи франшиз и маркетинг» и
   «2. Планы продаж и сезонность», который я так подписал 11.09.2026. В
   исходнике этот блок номера не имел вовсе.
2. Подпись `H6` обещала «Раздел 14–16: CAPEX и индексация», а вела в
   раздел 14. Пока разделов было четырнадцать, это читалось как «блоки
   внутри 14»; после появления разделов 15, 16 и 17 подпись стала прямой
   неправдой.
3. На разделы 15, 16 и 17 ссылок не было вовсе.

Причина
: нумерацию я не сверял со списком уже занятых номеров, а навигацию в
шапке — не трогал, добавляя разделы в конец листа.

Решение
: блок планов продаж стал «2а.» — подблок раздела 2, по образцу «5а.» у
согласованного мерча. Теперь 19 разделов и ни одного дубля номера.
Подпись `H6` исправлена на «Раздел 14: CAPEX и индексация», добавлены
`G8` → раздел 15–16 и `H8` → раздел 17. Список разделов для ссылок
собирается по заголовкам колонки A, а не зашит номерами строк, поэтому
следующий новый раздел не сломает навигацию.

Заодно исправлен способ задания ссылки: строка, присвоенная в
`cell.hyperlink`, становится **внешним** адресом, и Excel по ней никуда не
переходит. Внутренняя ссылка требует `Hyperlink(location=...)`. По этой же
причине не работала ссылка «переключить →» на дашборде V9.0, которую я
добавил утром, — исправлена.

Финансовый эффект
: нулевой. Подпись, номер раздела и гиперссылка в расчёт не входят.
Эталон совпал без `--update`: EBITDA 731 487 728, деньги на конец
451 069 418.

Проверки
: 49 тестов, `compare_v8_v9.py` 59 из 59, собственные контроли модели
проходят. Все восемь ссылок шапки проверены разрешением цели: каждая
попадает в заголовок, который обещает.

Справка по вопросу
: БИБОСС, TopFranchise и Forbes — не из модели v4, а из ТЗ, пункт 7:
«Ежегодные размещения: БИБОСС 250 тыс. ₽ в октябре, TopFranchise 250 тыс. ₽
в ноябре, Forbes 250 тыс. ₽ в декабре». В книге у них и стоит источник
«ТЗ 7». Из v4 пришли двенадцать строк — CAPEX (`B286:B290`, «v4:
Допущения!B41:B42») и инфляция по годам (`B301:B307`, «v4 / ежегодный
план»); из V5 — пять строк, включая `B52`, тот самый минимальный роялти
15 000 ₽, который 11.09.2026 заменён расчётом по группам из реестра.
Самого файла v4 в репозитории нет, только ссылки на него внутри книги.

---

## 11.09.2026 — кассовый разрыв: клубу платили до нуля на счёте

Проблема
: модель двенадцать месяцев подряд жила с отрицательным расчётным счётом,
глубиной до −8 707 185 ₽. Строки привлечения денег в БДДС нет, значит
закрыть этот минус нечем — модель показывала невозможное.

Причина
: `01_Вводные!B237` «Минимальный остаток денег при возврате займа» стоял
нулём, а правило возврата — `02_Модель!93 = MAX(0; остаток − B237)`. Ноль
означал «платим клубу, пока на счёте есть хоть что-то». В первый же месяц
модель отдавала клубу 3 075 292 ₽, имея на старте 187 878 ₽, и остаток
становился ровно нулём. Дальше любой платёж уходил в минус, а на
двадцать втором месяце совпал с квартальной закупкой склада на 34,5 млн —
отсюда провал на 8,7 млн.

Это не дефицит бизнеса, а порядок платежей: деньги отдавались по
необязательному обязательству раньше, чем по обязательному.

Решение
: `B237` = 10 млн ₽ (решение владельца модели 11.09.2026 из четырёх
посчитанных вариантов). Овердрафт решено не заводить: при таком пороге
линия почти не нужна.

Проверенная чувствительность к порогу:

| порог `B237` | мин. касса | месяцев в минусе | погашено клубу |
|---:|---:|---:|---:|
| 0 (было) | −8 707 185 | 12 | 50 млн |
| **10 млн (принято)** | **−1 638 237** | **2** | **50 млн** |
| 15 млн | −1 176 489 | 2 | 50 млн |
| 20 млн | +739 720 | 0 | 50 млн |
| 25 млн | +894 718 | 0 | 50 млн |

Та же правка внесена в V9.0: её формула погашения порога не знала вовсе —
`MAX(0; деньги_до_займа)` без вычитания. Теперь
`MAX(0; деньги_до_займа − МинОстатокДляЗайма)`, и книги снова сходятся.

Финансовый эффект
: **нулевой по экономике и по итогу горизонта.** Меняется только порядок
платежей: клубу отдаются те же 50 000 000 ₽, просто позже.

| показатель | было | стало |
|---|---:|---:|
| EBITDA | 731 487 728 | 731 487 728 |
| Выручка без НДС | 2 181 352 193 | 2 181 352 193 |
| Деньги на конец | 451 069 418 | 451 069 418 |
| Погашение займа | 50 000 000 | 50 000 000 |
| **Минимальный остаток** | **−8 707 185** | **−1 638 237** |
| Месяцев с минусом | 12 | 2 |

Что осталось нерешённым
: остаточный минус −1,64 млн в июне–июле 2028 — месяц крупной квартальной
закупки склада. Убирается порогом 20 млн (минус исчезает совсем) или
целевым запасом формы 3 месяца вместо 4 (+84 млн денег на конец, EBITDA
не меняется). Отдельно: даже после правки минимум первого года +894 тыс. ₽
при расходах около 10 млн в месяц — формально плюс, фактически подушки
нет. Компания стартует со 187 878 ₽ после сезона с убытком 23,7 млн;
это вопрос финансирования запуска, а не модели.

Проверки
: новый файл `tests/test_кассовый_разрыв.py` — четыре теста: порог не
нулевой; в месяц платежа клубу на счёте остаётся не меньше порога; касса
не проваливается глубже недельных расходов первого года; первый месяц не
заканчивается нулём на счёте. В `selfcheck.py` дефект «пустой счёт»:
порог снимается обратно в ноль — ловится тремя тестами из четырёх.

Первая версия двух тестов была беззубой: они сверяли модель с тем же
параметром, который и ломается, и при нулевом пороге проходили вхолостую.
Поймал это только `selfcheck` — ради него он и написан. Всего 53 теста,
аудит 1 019 находок и 0 важных, `compare_v8_v9.py` 59 из 59.

---

## 11.09.2026 — целевой запас формы 3 месяца: кассовый минус закрыт

Проблема
: после установки порога остатка 10 млн в модели оставались два месяца с
отрицательным счётом — июнь и июль 2028, глубина −1 638 237 ₽. Это месяц
квартальной закупки склада: 19,9 млн одним платежом.

Причина
: закупка идёт «до целевого запаса» раз в квартал (окна `B82:B85` =
2/5/8/11). При цели в четыре месяца будущих продаж один платёж
закрывает весь старт сезона, и никакой разумный порог остатка его не
перекрывает.

Решение
: целевой запас `B80` 4 → 3 месяца (решение владельца модели 11.09.2026).

Вместе с этим `B310` «Минимальный запас для возврата займа» опущен с 4 до
3 — и это не косметика, а необходимость. При цели 3 склад до четырёх
месяцев не дорастает никогда, порог становится недостижимым, и займ клубу
перестаёт гаситься: 6 106 954 ₽ за пять лет вместо 50 млн. Проверено
прогоном:

| запас / порог займа | мин. касса | мес. в минусе | погашено клубу | деньги на конец |
|---|---:|---:|---:|---:|
| 4 / 4 (было) | −1 638 237 | 2 | 50 000 000 | 451 069 418 |
| 3 / 4 | +894 718 | 0 | **6 106 954** | 542 506 457 |
| **3 / 3 (принято)** | **+894 718** | **0** | **50 000 000** | **498 613 411** |

Финансовый эффект
: экономика не меняется, оборотка высвобождается.

| показатель | было | стало |
|---|---:|---:|
| EBITDA | 731 487 728 | 731 487 728 |
| Выручка без НДС | 2 181 352 193 | 2 181 352 193 |
| Погашение займа клубу | 50 000 000 | 50 000 000 |
| **Деньги на конец** | 451 069 418 | **498 613 411** (+47 543 993) |
| **Минимальный остаток** | −1 638 237 | **+894 718** |
| Месяцев с минусом | 2 | **0** |

Минимальный склад остаётся положительным — 1 629 477 ₽ по себестоимости,
дефицита формы нет.

Попутно исправлена арифметика в моём же объяснении порога остатка. Я
назвал 20 млн «двумя месяцами операционных расходов», взяв за базу все
денежные выплаты (9,99 млн/мес, включая закупку склада, выплаты клубу и
налоги). Собственные операционные расходы модели в первый год —
6,80 млн/мес (ФОТ 4,80 + прочий OPEX 2,00), а по факту сезона 25/26
административные расходы 3,47 млн/мес. Против них 20 млн — это
три месяца по модели и почти шесть по факту. Владелец модели заметил
расхождение раньше, чем я.

Проверки
: `tests/test_кассовый_разрыв.py` ужесточён — вместо «минус не глубже
недельных расходов» теперь `test_счёт_не_уходит_в_минус`: ни одного
отрицательного месяца из шестидесяти. Добавлен
`test_порог_возврата_займа_не_выше_целевого_запаса` — он ловит ту самую
ловушку. В `selfcheck.py` новый дефект «порог займа»: `B310` поднимается
до 4 — ловится. На `08_Проверки` V9.0 появился восьмой контроль
«Порог возврата займа не выше целевого запаса». Всего 54 теста,
`compare_v8_v9.py` 59 из 59.

---

## 11.09.2026 — итоговая проверка: две ошибки в показателях дашборда

Проблема
: при сквозной проверке модели нашлись два показателя, которые считались
неверно и попадали прямо на дашборд.

1. **«Дети на конец периода» = 3 775.** Это август — дно сезонности
   (индекс посещаемости 6 %). В среднем за пятый год сеть обслуживает
   41 930 детей, на пике 58 502. Дашборд показывал цифру в одиннадцать раз
   меньше реальной, и именно её руководитель видит первой.
2. **«EBITDA, % выручки» за год** считалась как среднее из двенадцати
   помесячных долей. За первый год это давало −15,3 % вместо честных
   −7,5 %: разница 7,9 процентных пункта.

Причина
: обе строки — агрегаты, и обе агрегировались неправильным способом.
Дети — поток с сильной сезонностью, снимок конца периода для них
бессмыслен. Доля — отношение, и усреднять отношения по месяцам нельзя:
нужно делить сумму на сумму.

Решение
: в `v9_lib.Лист.ряд()` добавлен тип итога `доля` с параметрами
`делимое`/`делитель` — годовая колонка считается как отношение годовых
сумм. На него переведена строка «EBITDA, % выручки». На дашборде строка
детей переименована в «Дети, среднее за период» и считается средним за
год, а не снимком последнего месяца.

| год | 1 | 2 | 3 | 4 | 5 |
|---|---:|---:|---:|---:|---:|
| маржа, было | −15,3 % | 3,5 % | 24,4 % | 39,6 % | 47,8 % |
| маржа, стало | −7,5 % | 8,7 % | 26,0 % | 38,7 % | 45,2 % |
| дети, было | 401 | 998 | 1 999 | 2 952 | 3 775 |
| дети, стало | 3 967 | 7 936 | 18 116 | 30 654 | 41 930 |

Финансовый эффект
: нулевой. Обе строки производные, ни во что не входят. Эталон совпал без
`--update`, `compare_v8_v9.py` 59 из 59.

Проверки
: 54 теста, аудит 1 017 находок и 0 важных, `check_tz.py` 61 совпадение,
прогон V9.0 без ошибок в ячейках и без проваленных контролей.

---

## 11.09.2026 — переключатель сценария на дашборде

Проблема
: сценарий был виден только как текст в подзаголовке («Сценарий: Базовый»)
и переключался на листе вводных, до которого надо было доскроллить. Рядом
уже стоял переключатель истории — сценарий просился туда же.

Решение
: два переключателя в один блок справа от заголовка. Каждый показывает
текущее значение и даёт ссылку прямо в нужную ячейку вводных: сценарий —
в `B192`, история — в `B322`. Из подзаголовка сценарий убран: там осталось
то, что не переключается — дата старта и горизонт.

Под ними строка-пояснение, которая разводит две вещи, которые легко
перепутать: **сценарий пересчитывает всю модель, история — только
показ.**

Финансовый эффект
: нулевой. Переключатели показывают значения и ведут ссылками, в расчёт
не входят.

Проверки
: 54 теста, `compare_v8_v9.py` 59 из 59. Прогон V9.0 во всех трёх
сценариях: переключатель показывает «Негативный» (EBITDA 424,8 млн),
«Базовый» (731,5 млн), «Позитивный» (1 105,1 млн), ошибок в ячейках нет.

---

## 11.09.2026 — порог минимального остатка держится весь сезон

Проблема
: второй год модели показывал чистый денежный поток **−41 871 ₽** при
EBITDA 18,6 млн. Минус мизерный, но именно поэтому сбивает: читается как
ошибка расчёта. Причина — `02_Модель!93`: «доступный cash» проверял порог
`B237` только в тот месяц, когда клубу что-то платят. Дальше наступал
август (индекс посещаемости 6 %, аренда и ФОТ идут как обычно), счёт
проваливался и уходил ниже порога: 24-й месяц закрывался на 9 958 129 ₽
при пороге 10 000 000. Год начинался ровно на пороге и заканчивался чуть
ниже него — вот и весь минус.

Причина
: порог был ограничением на платёж, а не на остаток. Поднять сам порог не
помогало: он сдвигает обе границы года одинаково, и −41 871 остаётся на
месте.

Решение
: рядом со строкой «доступный cash» появился резерв на ближайшую
просадку — сколько съедят будущие убыточные месяцы, если считать их без
платежей клубу. Считается рекурсией с конца горизонта:

```
резерв(m) = MAX(0; резерв(m+1) − поток_до_займа(m+1))
```

Цикла нет: поток до займа от возврата займа не зависит. Клубу платим
только тем, что выше порога **и** выше этого резерва. Строка заведена в
витрине (`02_Модель!94`) и во всех пяти копиях движка — 720 формул.

Финансовый эффект
: EBITDA, выручка, дети и школы — без изменений. Деньги на конец
498 613 411 ₽, минимальный остаток +894 718 ₽ — тоже без изменений.
Клубу за горизонт возвращаются те же 50 млн: 41 871 ₽ переезжают со
второго года на четвёртый. Поток второго года стал ровно 0, счёт с 12-го
месяца ни разу не опускается ниже 10 млн.

| Год | было | стало |
|---|---:|---:|
| 1 | 9 812 122 | 9 812 122 |
| 2 | **−41 871** | **0** |
| 3 | 32 951 332 | 32 951 332 |
| 4 | 157 484 937 | 157 443 066 |
| 5 | 298 219 013 | 298 219 013 |

Негативный сценарий это не лечит: там минус в кассе операционный (девять
месяцев, до −7,5 млн), резерв не создаёт денег, которых нет. Вопрос, чем
финансируется кассовый разрыв в негативном сценарии, остаётся за
владельцем модели.

Проверки
: новый тест `test_порог_держится_после_первого_платежа_клубу` — после
первого платежа клубу остаток не падает ниже порога ни в одном месяце.
Соседний тест сверял только месяцы платежей и такую просадку пропускал.
Новый дефект `selfcheck` «просадка» (резерв обнулён) — пойман. Дефект
«пустой счёт» теперь ловится проверкой самого порога и эталоном: с
резервом один ноль в `B237` в минус счёт уже не уводит. 55 тестов,
`compare_v8_v9.py` 59 из 59, аудит без важных находок.

---

## 11.09.2026 — графики: оси подписаны, легенда не налезает

Проблема
: легенда садилась справа поверх области построения и закрывала пятый
год. Что отложено по осям — не сказано нигде: миллионы, штуки или люди,
понимай как хочешь. График «Деньги на конец месяца — где кассовый разрыв»
после закрытия разрыва показывал ровную растущую линию и не говорил
ничего.

Решение
: легенда вынесена под график (`legend.position = "b"`, `overlay = 0`),
у каждой оси своя подпись — включая правую на двухосевых графиках. График
кассового разрыва убран, осталось семь. Над каждым — строка курсивом, что
на нём смотреть: не повтор заголовка, а то, что из графика следует
(«открытий больше, чем продаж: один партнёр открывает несколько школ»).

Двухосевым графикам понадобилась своя ось категорий с отдельным номером:
без неё LibreOffice достраивал её сам — сверху, с чужой подписью «Год
модели» над заголовком. Подписи значений разведены: столбцам `outEnd`,
линиям `t`, иначе цифра линии садилась ровно на цифру столбца.

Заодно исправлена рентабельность EBITDA на дашборде: формула
`=IF(I6=0,0,I7/I6)` держала номера строк зашитыми, строки съехали, и
дашборд показывал 288,9 % — это роялти к паушальным, а не EBITDA к
выручке. Теперь номера берутся по названию строки, как и всё остальное на
листе: 33,5 %.

Финансовый эффект
: нулевой в расчёте; на дашборде исправлена цифра, которая вводила в
заблуждение (288,9 % → 33,5 %).

Проверки
: 55 тестов, `compare_v8_v9.py` 59 из 59, визуальная сверка через PDF.

---

## 11.09.2026 — лист вводных: строки по тексту, а не наугад

Проблема
: 71 строка на `01_Вводные` имела зашитую высоту 44–90 пунктов —
комментарий в узкой колонке E переносился на три-четыре строки. Таблица
читалась как лестница: полэкрана на десяток параметров.

Решение
: колонка комментария расширена до 90 символов, высота каждой строки
пересчитывается по длине текста в переносимых ячейках, потолок — три
строки. Строк выше 30 пунктов осталось 6 вместо 71.

Финансовый эффект
: нулевой, меняется только представление.

Проверки
: контроль сборки — 0 незапланированных изменений формул, 55 тестов.

---

## 11.09.2026 — срок закрытия займа клубу на дашборде

Проблема
: сколько лет отдаётся займ, приходилось вычитать глазами из графика
«Займ клубу» или искать первый ноль в строке «Займ на конец» на
`06_Деньги`. На дашборде этого числа не было.

Решение
: строка в блоке «На что смотреть»: **«Срок закрытия займа клубу,
мес.»**. Займ гасится монотонно — он только уменьшается, — поэтому число
месяцев с ненулевым остатком и есть срок: долг закрыт в следующем.
Формула `COUNTIF(остаток>0,01)+1`, без массивов. Рядом, в колонке D, —
тот же месяц календарём (`INDEX` по строке дат, не `TEXT`: формат внутри
`TEXT` читается по локали Excel и в другой ломается).

Если за горизонт займ не закрылся, строка честно пишет «не закрыт за
60 мес.» вместо «61». Проверено подстановкой `B310 = 6`: при пороге выше
целевого запаса склад до него не дорастает, гашение не идёт вовсе, и
строка показывает именно это.

Финансовый эффект
: нулевой. Строка только показывает то, что и так считалось.

Проверки
: базовый прогон — 48 месяцев (август 2030), сверено с рядом «Займ на
конец»: последний месяц с долгом 47-й, остаток 1 884 305 ₽, в 48-м ноль.
55 тестов, `compare_v8_v9.py` 59 из 59.

---

## 11.09.2026 — конверсия продажи в открытие 100 % → 85 %

Проблема
: `01_Вводные!B298` стоял на 100 %. Это означало, что ни одна проданная
франшиза не срывается: партнёр всегда находит площадку, всегда
согласовывает её и всегда доводит открытие до конца. Такого не бывает — в
v4 этот параметр был сценарным и не поднимался выше 97 % даже в
позитивном сценарии.

Причина
: при переносе в текущую модель конверсия стала штатной вводной и была
поставлена в 100 % как нейтральное значение «до решения владельца».

Решение
: 85 % — по решению владельца 11.09.2026. Для справки: в v4 это значение
негативного сценария, в базовом там стояло 95 %. Взято сознательно как
консервативное.

Финансовый эффект
: за пять лет

| Показатель | было (100 %) | стало (85 %) | дельта |
|---|---:|---:|---:|
| Выручка без НДС, ₽ | 2 181 352 193 | 1 948 443 373 | −232 908 819 |
| EBITDA, ₽ | 731 487 728 | 599 036 354 | −132 451 373 |
| Рентабельность EBITDA | 33,5 % | 30,7 % | −2,8 п. п. |
| Новые школы на конец | 425 | 362 | −64 |
| Деньги на конец, ₽ | 498 613 411 | 400 539 937 | −98 073 474 |
| Минимальный остаток, ₽ | +894 718 | +723 716 | −171 002 |

Кассового разрыва не появляется: отрицательных месяцев по-прежнему нет.
Клубу возвращаются те же 50 млн, срок закрытия займа тот же — 48 месяцев
(август 2030). Продажи не изменились: 320 сделок, сокращается только доля
дошедших до открытия.

Проверки
: 55 тестов, `compare_v8_v9.py` 59 из 59, эталон обновлён с этим
объяснением.
